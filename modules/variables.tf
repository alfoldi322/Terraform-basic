#variable "db_password" {
#  default = "${var.db_password}"
#}

variable "access_key" {
  default = "${var.access_key}"
}
variable "secret_key" {
  default = "${var.secret_key}"
}
variable "region" {
  default = "${var.region}"
}